//
//  DigitManagerTests.swift
//  CalculatorKitTests
//
//  Created by 서상의 on 2020/11/24.
//

import XCTest

// TODO: Making allTests constant variable.
class DigitManagerTests: XCTestCase {
    // MARK: Test Logic 1:
}

// MARK: - Digit Store
// - 최대 저장 가능한 숫자의 길이를 가질 수 있음
// - `String` 타입의 한 자리 숫자를 저장할 수 있음
//      - 유효한 숫자인지에 대한 검사를 할 수 있음
//          - 숫자
//          - 이미 저장된 숫자의 갯수
// - 이는 `[String]` 타입의 배열에 저장됨
// - 소수점을 저장할 수 있음
//      - 저장된 숫자의 갯수에 포함되지 않음
// - 숫자의 부호 종류를 설정할 수 있음
//      - 초기값은 `.plus`, 양수
// - `Double` 타입의 숫자를 반환할 수 있음
//      - 입력 메소드에서 값을 검증하므로 별도의 에러 처리는 필요하지 않음
//      - 부호의 종류에 따라 배열 앞에 가장 앞에 적당한 부호를 입력함

// Digit.Store
// Digit.Manager
